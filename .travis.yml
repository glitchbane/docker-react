language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t glitchbane/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true glitchbane/docker-react npm run test -- --coverage